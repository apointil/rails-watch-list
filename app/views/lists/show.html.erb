<div class="container">
  <div class="card-category mb-4 mt-1" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(https://s3-us-west-2.amazonaws.com/prd-rteditorial/wp-content/uploads/2018/03/13153742/RT_300EssentialMovies_700X250.jpg)">
    <%= @list.name %>
  </div>
  <% @list.bookmarks.each do |bookmark| %>
  <%# faire des cards %>
  <p><%= bookmark.comment %> - <em><%= bookmark.movie.title %></em> -
  <%= link_to 'delete', bookmark_path(bookmark), method: :delete, data: { confirm: "Are you sure?" }  %></p>

  <% end %>

  <%= link_to 'Add a bookmark', new_list_bookmark_path(@list), class: "btn btn-primary"  %>
  <%= link_to 'Back to lists', lists_path, class: "btn btn-primary"  %>
</div>
